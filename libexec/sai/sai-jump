#!/usr/bin/env bash
### sai-jump -- update the spread
### Usage: sai jump <new-gap>
set -e
[ $# = 1 ] || sai jump --usage
wad=$(sai wad -h "$1")
echo >&2 "Setting gap to $wad..."
wad=$(sai wad "$1")
(set -x; seth send "${SAI_TAP?}" "jump(uint128)" "$wad")
